# 🚀 数据迁移工具 - 独立版本使用说明

## 📦 文件说明

这是数据迁移工具的独立可执行版本，包含以下文件：

```
data-migration-tool/
├── data-migration-tool.exe    # 主程序（Windows）
├── run.bat                    # 启动脚本（Windows）
├── web/                       # Web资源文件夹
│   ├── static/               # 静态资源（CSS、JS、图片）
│   └── templates/            # HTML模板
└── STANDALONE_README.md      # 本说明文件
```

## 🚀 快速开始

### 方式一：使用PowerShell脚本（推荐）
1. 右键点击 `run.ps1` 文件
2. 选择"使用PowerShell运行"
3. 程序会自动检查环境并启动
4. 打开浏览器访问 `http://localhost:8080`

### 方式二：使用批处理脚本
1. 双击 `run.bat` 文件
2. 程序会自动检查环境并启动
3. 打开浏览器访问 `http://localhost:8080`

**注意**: 如果在Windows命令行中看到中文乱码，请使用PowerShell脚本或直接运行exe文件。

### 方式三：直接运行
1. 双击 `data-migration-tool.exe`
2. 或在命令行中运行：
   ```cmd
   data-migration-tool.exe
   ```
3. 打开浏览器访问 `http://localhost:8080`

## 📁 自动初始化

程序首次运行时会自动创建以下目录和文件：

```
./data/                        # 数据存储目录
├── datasources.json          # 数据源配置（空）
├── filter_templates.json     # 过滤模板（包含示例）
├── masking_rules.json        # 脱敏规则（包含示例）
├── masking_types.json        # 脱敏类型定义
├── table_migrations.json     # 表迁移配置
├── migration_tasks.json      # 迁移任务记录
└── migration_records.json    # 迁移执行记录

./logs/                        # 日志目录（自动创建）
```

## ⚙️ 环境变量配置

可以通过设置环境变量来自定义程序行为：

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `PORT` | 8080 | Web服务端口 |
| `DATA_DIR` | ./data | 数据存储目录 |
| `LOG_LEVEL` | info | 日志级别 |
| `STATUS_CHECK_INTERVAL` | 300 | 数据源状态检查间隔（秒） |

### Windows 设置环境变量示例：
```cmd
set PORT=9090
set DATA_DIR=D:\migration-data
data-migration-tool.exe
```

## 🎯 功能特性

### 🔄 数据迁移
- **多数据库支持**: MySQL ↔ PostgreSQL
- **多表迁移**: 一次任务迁移多张表
- **并发处理**: 多级并发控制，提升迁移效率
- **断点续传**: 支持任务暂停和恢复

### 🔍 智能过滤
- **条件过滤**: 支持复杂的WHERE条件
- **过滤模板**: 可复用的过滤条件模板
- **多种操作符**: =, !=, >, <, >=, <=, LIKE, IN等

### 🛡️ 数据脱敏
- **通用模板**: 保留首尾、保留开头、保留结尾、全部脱敏
- **常用类型**: 手机号、邮箱、身份证、银行卡号脱敏
- **个人信息**: 姓名、中文姓名、地址、护照脱敏
- **企业信息**: 企业名称、统一社会信用代码、车牌号脱敏
- **网络信息**: IP地址脱敏
- **自定义规则**: 支持正则表达式自定义脱敏逻辑
- **字段级控制**: 精确控制每个字段的脱敏方式

### 📊 任务管理
- **实时监控**: 任务执行进度实时显示
- **状态管理**: 支持启动、暂停、停止、重新运行
- **详细日志**: 完整的执行日志和错误信息
- **批量操作**: 支持批量管理多个任务

## 🔧 故障排除

### 常见问题

**1. 程序无法启动**
- 检查端口8080是否被占用
- 尝试以管理员身份运行
- 检查防火墙设置

**2. 无法访问Web界面**
- 确认程序已正常启动
- 检查浏览器地址是否正确：`http://localhost:8080`
- 尝试使用其他浏览器

**3. 数据库连接失败**
- 检查数据库服务是否运行
- 验证连接信息（主机、端口、用户名、密码）
- 确认网络连通性和防火墙设置

**4. 迁移任务失败**
- 查看详细错误日志
- 检查源表和目标表结构
- 验证数据库权限设置

### 日志查看

程序运行日志保存在 `./logs/` 目录中：
- 访问日志：记录Web请求信息
- 错误日志：记录程序错误和异常
- 迁移日志：记录数据迁移详细过程

## 📞 技术支持

### 获取帮助
- 📋 [GitHub Issues](https://github.com/hcr707305003/data-migration-tool/issues) - 报告问题
- 💬 [GitHub Discussions](https://github.com/hcr707305003/data-migration-tool/discussions) - 社区讨论
- 📧 Email: support@example.com - 技术支持

### 版本信息
- 当前版本：v1.2.0
- 构建时间：查看程序启动时的显示信息
- 更新检查：访问 [GitHub仓库](https://github.com/hcr707305003/data-migration-tool) 查看最新版本

## 🔒 安全建议

1. **网络安全**
   - 建议在内网环境中使用
   - 生产环境请配置防火墙规则

2. **数据安全**
   - 迁移前务必备份重要数据
   - 使用强密码保护数据库连接
   - 敏感数据请配置脱敏规则

3. **权限控制**
   - 使用最小权限原则配置数据库账号
   - 定期更换数据库密码

## 📈 性能优化

1. **并发设置**
   - 根据服务器性能调整并发参数
   - 避免过高并发导致数据库压力

2. **批处理大小**
   - 大数据量迁移时适当增加批处理大小
   - 内存不足时减少批处理大小

3. **网络优化**
   - 数据库间网络延迟高时增加批处理大小
   - 使用高速网络连接提升传输效率

---

**感谢使用数据迁移工具！如有问题请及时反馈。** 🙏